#include <stdio.h>

int main()
{
    int x;
    scanf("%d", &x);

    int polynomial;

    polynomial = ( ( ( (3*x + 2) *x - 5) * x - 1 ) * x + 7) * x -6;
    
    printf("The value of the polynomial is: %d", polynomial);

    return 0;
    
}